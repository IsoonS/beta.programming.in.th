#include<iostream>
using namespace std;
int main()
{
  int a, b, c;
  int in_sq_root = b * b - 4 * a * c;
  cin >> a >> b >> c;
  if (in_sq_root >= 0) {

  } else {
    cout << "No Solution";
  }
  return 0;
}